# Enable italic text (if your terminal supports it)
--italic-text=always

# Use gruvbox-dark theme
--theme="gruvbox-dark"

# Show line numbers, Git changes, and file header
--style="numbers,changes,header"

# Always show the header with file info
#--decorations=always

# Use a pager if output is longer than terminal height
--paging=auto
